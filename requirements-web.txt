# The web interface as deployed (see Dockerfile): the versions the test suite
# passed with. DECIMER goes into an environment of its own
# (requirements-decimer.txt).
rdkit==2026.3.4
numpy==2.4.6
pillow==12.3.0
pyyaml==6.0.3
gemmi==0.7.5
fastapi==0.141.1
starlette==1.3.1
pydantic==2.13.5
uvicorn==0.52.0
python-multipart==0.0.32
itsdangerous==2.2.0
